import { TienePermisoPipe } from './tiene-permiso.pipe';

describe('TienePermisoPipe', () => {
  it('create an instance', () => {
    const pipe = new TienePermisoPipe();
    expect(pipe).toBeTruthy();
  });
});
